//Tasks

1. Build function to handle end case that city not in range received in InputProcedure.
2. Invesigate whether to leave the local variable "currAdjCityNode"
and use directly "GetCurrAdjCityNode()" 
If so -> while creating "Next" ItemType , send to next node in list?
 